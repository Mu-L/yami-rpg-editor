- yami-rpg-editor: 类型化时绝不能为绕编译错误而注释掉 `Command.parse = CommandSchema.parse` 等运行时绑定——循环依赖应通过 module-init.js 加载顺序（head.html SOT）保证，注释绑定会导致运行时 `Command.parse is not a function` / `Command.words` 为 null
- yami-rpg-editor: `scripts/verify-imports.js` 的导出正则已含 `interface|type`（2026-07-23 改），类型化引入 `export interface`/`export type` 后可被识别校验
- yami-rpg-editor: WordList.push 必须返回 this（链式契约），多处调用方依赖 `words.push(x).push(y)` 链式。绕 TS2416 方案：WordList 不 extends Array，改独立类持 #arr 私有数组 + interface 声明合并保留索引访问语义
- yami-rpg-editor: TS class 字段语义陷阱——运行时 prototype 挂载的方法（module/global.ts 在 editor_loaded 用 `Xxx.prototype.method = function() {...}` 挂载）在 class 字段声明时必须用 `declare` 修饰符，绝不能写 `method: (...args: any[]) => any;` 无初值声明，否则 TS 发射 `this.method = undefined` 初始化覆盖 prototype 挂载，运行时报 `is not a function`。本轮 command-list.ts:41 openEdit 走此坑
- yami-rpg-editor: inspector.ts 的 processors['inspector-param-*'] 4处必须调 ParamListHistory.static restore(list, data, type, operation) 4参静态方法（真执行 splice/enableItems undo/redo 逻辑）；不可改调 history.restore(operation) 实例转发——history 是 ParamHistory 实例（非 ParamListHistory），其 restore(operation) 转发 this.owner.history.restore 会造成 History.restore→processors→history.restore 闭环无限递归（2026-07-23 报错 history.ts:49 Maximum call stack size exceeded）
