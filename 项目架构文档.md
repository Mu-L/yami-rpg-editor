# Yami RPG Editor 项目架构文档

## 项目概述

Yami RPG Editor 是一款基于 Electron 的 2D RPG 游戏编辑器，以用户友好性著称。项目使用 JavaScript、Node.js 和 Electron 技术栈构建。

## 整体架构

项目采用 Electron 架构，分为主进程和渲染进程两部分：

- **主进程（Main Process）**：负责应用程序的生命周期管理、窗口管理、文件系统操作、IPC 通信等
- **渲染进程（Renderer Process）**：负责编辑器的用户界面和编辑功能

### 目录结构

```
/
├── main/                    # Electron 主进程文件
├── bin/                     # 构建工具脚本
├── Project/                 # 编辑器核心资源
│   ├── Script/             # 编辑器 JavaScript 模块
│   │   ├── module/         # 功能子模块
│   │   └── *.js            # 核心编辑器脚本
│   ├── Templates/          # 项目模板
│   ├── Fonts/              # 字体资源
│   ├── Images/             # 图像资源
│   ├── Locales/            # 多语言文件
│   └── vs/                 # Monaco Editor（代码编辑器）
├── Runtime/                 # 运行时依赖
├── Unused/                  # 未使用的代码
└── package.json            # 项目配置文件
```

## 主进程文件（main/）

### main/main.js
**功能**：Electron 主进程入口文件

**主要职责**：
- 应用程序生命周期管理（启动、关闭、退出）
- 创建和管理编辑器窗口和播放器窗口
- 文件系统操作（读写文件、目录管理）
- 本地开发服务器（基于 Koa，用于预览游戏）
- IPC 通信处理（与渲染进程通信）
- TypeScript 编译服务（TSC watch 模式）
- Excel 导入导出功能（多语言翻译）
- 二维码生成（用于移动端预览）
- 菜单栏创建和管理
- APK 构建流程协调

**核心功能模块**：
- 窗口管理：创建编辑器窗口、播放器窗口，处理窗口状态
- 文件系统：异步文件读写、保护承诺队列防止数据丢失
- 开发服务器：启动/停止本地 Web 服务器，用于游戏预览
- TypeScript 编译：启动 TSC watch 模式，实时编译 TypeScript 代码
- Excel 处理：导出/导入多语言翻译文件
- IPC 事件处理：处理来自渲染进程的各种请求

### main/apk.js
**功能**：Android APK 构建和签名处理

**主要职责**：
- APK 反编译（使用 apktool）
- 修改 AndroidManifest.xml（包名、版本信息）
- 替换应用图标（支持多种尺寸和密度）
- 修改应用名称（多语言支持）
- 重新编译 APK
- APK 对齐（zipalign）
- APK 签名（apksigner/jarsigner）
- 资源文件合并
- 构建进度跟踪和错误处理

**核心功能**：
- 完整的 APK 重新打包流程
- 图标尺寸自动调整（使用 sharp 库）
- 签名验证
- 构建任务可中断
- 详细的错误日志

## 构建工具（bin/）

### bin/buildres.js
**功能**：资源打包工具

**主要职责**：
- 将项目模板和资源打包成 ZIP 文件
- 计算文件哈希值（MD5、SHA1、SHA256）
- 生成资源包元数据（pack.json）
- 用于项目发布和资源更新

## 编辑器核心脚本（Project/Script/）

### main.js
**功能**：编辑器主入口和协调器

**主要职责**：
- 编辑器初始化流程
- 组件管理和初始化
- 项目打开/关闭/保存
- 配置文件加载和保存
- 编辑器更新检查
- 项目版本兼容性检查
- 编辑器状态管理（opened/closed）

**核心流程**：
1. 初始化所有编辑器组件
2. 加载编辑器配置
3. 检查更新
4. 管理项目生命周期

### data.js
**功能**：数据管理和加载

**主要职责**：
- 加载和管理项目数据文件
- 元数据清单管理（manifest.json）
- 数据映射表创建（GUID 映射、变量映射等）
- 预设数据注册和管理
- 脚本加载和执行
- 数据继承和同步

**管理的数据类型**：
- 场景数据、UI 数据、动画数据、粒子效果
- 角色、技能、物品、装备、状态
- 事件、脚本、缓动、队伍
- 变量、属性、枚举、本地化
- 插件、自定义指令、配置

### scene.js
**功能**：场景编辑器

**主要职责**：
- 场景地图编辑
- 瓦片地图（Tilemap）绘制和管理
- 图层管理
- 对象放置（事件、角色、区域、光源等）
- 场景预览和渲染
- 网格显示、光照显示
- 场景动画播放
- 历史记录（撤销/重做）
- 场景缩放和平移

**核心功能**：
- 多图层瓦片绘制
- 自动瓦片（Autotile）支持
- 对象选择和编辑
- 场景导航和缩放
- WebGL 渲染支持

### ui.js
**功能**：UI 界面编辑器

**主要职责**：
- UI 元素创建和编辑
- UI 布局管理
- 元素层级关系管理
- 控制点和变换操作
- 文本元素编辑
- 多语言文本更新
- 预制件（Prefab）管理
- 历史记录（撤销/重做）
- UI 缩放和平移

**核心功能**：
- 可视化 UI 编辑
- 元素拖放和调整大小
- 锚点和对齐
- 字体和样式管理
- 响应式布局支持

### animation.js
**功能**：动画编辑器

**主要职责**：
- 帧动画创建和编辑
- 时间轴管理
- 关键帧编辑
- 图层管理
- 动画播放控制
- 洋葱皮（Onionskin）显示
- 运动轨迹编辑
- 动画导出

**核心功能**：
- 基于时间轴的动画编辑
- 多图层支持
- 关键帧插值
- 动画预览和循环
- 控制点变换

### layout.js
**功能**：编辑器布局管理器

**主要职责**：
- 工作区布局管理
- 窗口分组和排列
- 拖放布局调整
- 布局方案保存和加载
- 焦点管理
- 导航栏管理
- 响应式布局调整

**核心功能**：
- 灵活的窗口布局系统
- 拖放窗口重新排列
- 自定义布局保存
- 默认布局恢复

### file.js
**功能**：文件系统抽象层

**主要职责**：
- 文件读取（图像、JSON、文本等）
- 文件保存和写入
- GUID 到路径的映射
- 文件路径解析
- 图像预加载和缓存
- 文件对话框显示
- 资源管理器集成

**核心功能**：
- 异步文件操作
- 文件承诺管理
- 路径规范化
- 文件大小格式化

### command.js
**功能**：游戏指令系统

**主要职责**：
- 游戏指令解析和编辑
- 指令语法高亮
- 变量和属性解析
- 指令参数管理
- 自定义指令支持
- 指令验证
- 工具提示生成

**核心功能**：
- 富文本指令编辑
- 变量引用解析
- 颜色编码
- 指令模板系统

### browser.js
**功能**：项目浏览器

**主要职责**：
- 项目文件树显示
- 文件和资源浏览
- 搜索功能
- 文件创建和删除
- 脚本打开和编辑
- 导航历史
- 搜索历史

**核心功能**：
- 树形文件浏览
- 搜索和过滤
- 上下文菜单
- 主题适配

### plugin.js
**功能**：插件管理器

**主要职责**：
- 插件加载和卸载
- 插件元数据解析
- 插件参数配置
- 插件脚本编辑
- 插件列表管理
- 插件预览

**核心功能**：
- 动态插件系统
- 参数化配置
- 脚本热重载

### local.js
**功能**：本地化和多语言支持

**主要职责**：
- 语言包加载
- 文本本地化
- 语言切换
- 本地化字符串管理
- 动态文本更新

### tools.js
**功能**：编辑器工具集

**主要职责**：
- 通用工具函数
- 数学计算
- 颜色处理
- 几何运算
- 数据转换

### util.js
**功能**：实用工具函数

**主要职责**：
- 字符串处理
- 对象操作
- 数组操作
- 时间格式化
- 深度克隆

### components.js
**功能**：UI 组件库

**主要职责**：
- 自定义 UI 组件定义
- 组件生命周期管理
- 事件绑定
- 组件通信

### sprite.js
**功能**：精灵和图像管理

**主要职责**：
- 精灵加载和缓存
- 图像裁剪和处理
- 精灵动画
- 图集管理

### palette.js
**功能**：调色板和资源面板

**主要职责**：
- 瓦片集显示
- 资源选择
- 预览功能
- 拖放支持

### particle.js
**功能**：粒子效果编辑器

**主要职责**：
- 粒子系统编辑
- 粒子参数配置
- 实时预览
- 粒子导出

### inspector.js
**功能**：属性检查器

**主要职责**：
- 对象属性显示和编辑
- 动态表单生成
- 属性验证
- 实时更新

### audio.js
**功能**：音频管理

**主要职责**：
- 音频播放和控制
- 音量管理
- 音频预加载
- BGM 和 SE 支持

### variable.js
**功能**：变量管理器

**主要职责**：
- 游戏变量定义
- 变量类型管理
- 变量引用追踪
- 变量编辑界面

### attribute.js
**功能**：属性系统管理器

**主要职责**：
- 游戏属性定义
- 属性组管理
- 属性标签系统
- 属性模板

### enum.js
**功能**：枚举类型管理器

**主要职责**：
- 枚举定义和编辑
- 枚举组管理
- 枚举值映射
- 枚举引用

### title.js
**功能**：标题栏管理

**主要职责**：
- 窗口标题更新
- 项目名称显示
- 修改状态提示
- 标题栏控制

### log.js
**功能**：日志系统

**主要职责**：
- 错误日志记录
- 控制台输出
- 异常捕获和报告
- 调试信息

### printer.js
**功能**：打印和富文本渲染

**主要职责**：
- 富文本解析
- 文本渲染
- 样式应用
- 文本标签处理

### codec.js
**功能**：编解码器

**主要职责**：
- 数据序列化
- 数据反序列化
- 格式转换
- 压缩和解压

### update.js
**功能**：更新系统

**主要职责**：
- 编辑器更新检查
- 版本对比
- 更新下载
- 更新日志显示

### webgl.js
**功能**：WebGL 渲染引擎

**主要职责**：
- WebGL 上下文管理
- 着色器管理
- 纹理管理
- 渲染优化

## 功能模块（Project/Script/module/）

### module/global.js
**功能**：全局配置和常量

**主要职责**：
- 全局变量定义
- 常量配置
- 系统参数

### module/eventbus.js
**功能**：事件总线

**主要职责**：
- 事件发布订阅
- 组件间通信
- 事件解耦

### module/net.js
**功能**：网络通信

**主要职责**：
- HTTP 请求
- 数据同步
- 远程资源加载

### module/resource.js
**功能**：资源管理器

**主要职责**：
- 资源加载和卸载
- 资源缓存
- 资源依赖管理
- 资源路径解析

### module/editdata.js
**功能**：编辑数据处理

**主要职责**：
- 编辑数据格式化
- 数据验证
- 数据转换

### module/apkbuilder.js
**功能**：APK 构建模块（渲染进程端）

**主要职责**：
- APK 构建界面
- 构建进度显示
- 构建配置管理
- 与主进程通信

### module/webserver.js
**功能**：Web 服务器管理（渲染进程端）

**主要职责**：
- 启动/停止开发服务器
- 服务器状态管理
- 预览 URL 生成
- 二维码显示

### module/eslints.js
**功能**：代码质量检查

**主要职责**：
- JavaScript/TypeScript 语法检查
- 代码风格检查
- 错误提示

### module/searchstring.js
**功能**：搜索功能

**主要职责**：
- 文本搜索
- 正则表达式搜索
- 搜索结果高亮
- 搜索历史

### module/settingconfig.js
**功能**：设置配置管理

**主要职责**：
- 编辑器设置
- 用户偏好保存
- 配置同步

### module/command/（自定义指令）
**功能**：扩展指令模块

包含：
- `requestURL.js`：网络请求指令
- `downloadFile.js`：文件下载指令
- `getObjectProperty.js`：对象属性获取指令

## Monaco Editor 集成（Project/vs/）

Monaco Editor 是 VS Code 的代码编辑器核心，集成在编辑器中用于：
- JavaScript/TypeScript 脚本编辑
- 语法高亮
- 代码补全
- 错误提示
- 代码格式化

**相关文件**：
- `vs/editor/editor.main.js`：编辑器主文件
- `vs/language/typescript/`：TypeScript 语言支持
- `vs/loader.js`：AMD 模块加载器

## 项目模板（Project/Templates/）

包含多个游戏项目模板：
- `arpg-ts-english`：ARPG TypeScript 英文模板
- `arpg-ts-chinese`：ARPG TypeScript 中文模板
- `electron-win-x64`：Windows Electron 运行时
- `electron-mac-universal.app`：macOS Electron 运行时

## 数据流

### 编辑器启动流程
1. 主进程启动（main/main.js）
2. 创建编辑器窗口
3. 加载 index.html
4. 初始化编辑器（main.js）
5. 加载配置和数据（data.js）
6. 初始化各个组件
7. 显示主界面

### 项目打开流程
1. 用户选择项目目录
2. 加载项目元数据（manifest.json）
3. 加载项目数据文件
4. 创建 GUID 映射
5. 初始化编辑器组件
6. 加载项目配置
7. 切换到项目工作区

### 文件保存流程
1. 用户触发保存操作
2. 收集需要保存的数据
3. 序列化数据为 JSON
4. 通过 IPC 发送到主进程
5. 主进程异步写入文件
6. 返回保存结果
7. 更新 UI 状态

### APK 构建流程
1. 用户配置 APK 参数
2. 通过 IPC 发送构建请求
3. 主进程调用 apk.js
4. 反编译原始 APK
5. 修改资源和配置
6. 复制项目资源
7. 重新编译 APK
8. 对齐和签名
9. 返回构建结果

## 技术栈

### 前端技术
- **JavaScript ES6+**：核心编程语言
- **jQuery**：DOM 操作和事件处理
- **Monaco Editor**：代码编辑器
- **WebGL**：高性能图形渲染
- **HTML5 Canvas**：2D 图形绘制

### 后端技术
- **Node.js**：JavaScript 运行时
- **Electron**：桌面应用框架
- **Koa**：Web 服务器框架
- **TypeScript**：类型化 JavaScript

### 核心依赖
- **axios**：HTTP 客户端
- **exceljs**：Excel 文件处理
- **sharp**：图像处理
- **archiver**：文件压缩
- **xml2js**：XML 解析
- **qrcode**：二维码生成
- **mime-types**：MIME 类型识别

### 开发工具
- **electron-builder**：应用打包
- **prettier**：代码格式化
- **husky**：Git 钩子
- **lint-staged**：暂存文件检查

## 架构特点

### 模块化设计
- 每个功能模块独立封装
- 清晰的职责分离
- 便于维护和扩展

### 事件驱动
- 组件间通过事件通信
- 松耦合架构
- 灵活的事件处理

### 数据驱动
- 数据与视图分离
- 统一的数据管理
- 响应式更新

### 插件系统
- 支持插件扩展
- 自定义指令
- 热重载支持

### 国际化
- 多语言支持
- 本地化资源管理
- 动态语言切换

## 性能优化

- 异步文件操作
- 图像预加载和缓存
- WebGL 硬件加速
- 延迟加载
- 内存管理
- 事件节流和防抖

## 总结

Yami RPG Editor 采用现代化的技术栈和清晰的架构设计，提供了完整的 2D RPG 游戏开发工具链。通过模块化的设计和丰富的功能模块，开发者可以高效地创建和编辑游戏内容。编辑器支持场景编辑、UI 设计、动画制作、脚本编写等全方位的游戏开发功能，并提供了 APK 打包、多平台导出等实用工具。
